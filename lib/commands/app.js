const has=require('./helpers/validators'),complete=require('./helpers/complete');module.exports=()=>{has.args(1),has.signed_in();let a=has.id(pgb.opts.commands[1]);return pgb.api.getApp(a).then((a)=>pgb.print({pretty:a,json:a,bare:a.id}))},module.exports.completion=complete.app;