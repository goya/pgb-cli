const has=require('./helpers/validators'),wait=require('./helpers/wait'),complete=require('./helpers/complete');module.exports=()=>{has.args(1);let a=has.id(pgb.opts.commands[1]);return has.signed_in(),pgb.api.getApp(a).then(wait).then(pgb.json)},module.exports.completion=complete.app;