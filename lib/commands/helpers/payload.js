const fs=require('fs');module.exports.addFiles=(a,...b)=>{for(let c of b){c=c.split(':');let b=c.shift(),d=pgb.opts.variables[b];if(d){let e=c.join('')||b;try{fs.closeSync(fs.openSync(d,'r')),a[e]=fs.createReadStream(d)}catch(a){throw new Error(`file "${d}" not found or cannot be read - ${a}`)}}}return a},module.exports.addVariables=(a,...b)=>{for(let c of b){c=c.split(':');let b=c.shift(),d=c.join('')||b;pgb.opts.variables[b.toLowerCase()]&&(a[d]=pgb.opts.variables[b.toLowerCase()])}return a};