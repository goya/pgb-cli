const has=require('./helpers/validators'),wait=require('./helpers/wait'),complete=require('./helpers/complete');module.exports=()=>{has.args(1),has.signed_in();let a=has.id(pgb.opts.commands[1]),b=has.platform(pgb.opts.commands[2],!1);return pgb.api.buildApp(a,b).then(wait).then(a=>pgb.print({bare:a.id,json:a}))},module.exports.completion=complete.appAndPlatform;